# First Version of Kafka Pack

first create, active *virtualenv*  and `pip install -r requirements.txt` after install run command `python setup.py install` for install local package.

## Basic usage
Step 1 : `python tests/con.py` to run consumer
Step 2 : `python tests/pro.py` to send message to consumer
